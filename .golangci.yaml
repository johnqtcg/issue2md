run:
  timeout: 5m
  tests: true

linters:
  enable:
    # static check
    - govet
    - staticcheck
    - unused
    - ineffassign
    - errcheck

    # naming/style
    - revive
    - misspell
    - gocritic

    # complexity / structure
    - gocyclo
    - dupl

    # quality enforcement
    - goconst
    - prealloc
    - unparam
    - nolintlint
    - gosec

  disable:
    - wsl
    - gochecknoglobals
    - gochecknoinits
    - cyclop
    - funlen

  settings:
    gocyclo:
      min-complexity: 15

    funlen:
      lines: 120
      statements: 80

    dupl:
      threshold: 120

    lll:
      line-length: 140

    revive:
      severity: warning
      rules:
        - name: var-naming
        - name: unused-parameter
        - name: receiver-naming
        - name: error-naming
        - name: exported
        - name: indent-error-flow
        - name: if-return
        - name: redefines-builtin-id

    misspell:
      locale: US

    gosec:
      includes:
        - G101 # hardcoded credentials
        - G201 # SQL string formatting
        - G301 # permissive directory permissions
        - G302 # permissive file permissions
        - G401 # weak cryptographic primitives
        - G402 # TLS InsecureSkipVerify
        - G501 # blocklisted imports (e.g., crypto/md5)

  exclusions:
    # skip vendor, tmp, and *_test.go files
    paths:
      - vendor
      - tmp
      - '(.+)_test\.go'

version: "2"
