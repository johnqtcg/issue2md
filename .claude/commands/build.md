---
allowed-tools: Bash(make:build)
---

Run `make build` to build the project.

If the build fails, analyze the error output and identify the root cause. Provide a clear explanation of what went wrong and suggest how to fix it.
